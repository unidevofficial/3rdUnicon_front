import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "문의 | UNICON 2025",
  description: "자주 묻는 질문과 연락처 안내",
}

export default function InquiryLayout({ children }: { children: React.ReactNode }) {
  return children
}
